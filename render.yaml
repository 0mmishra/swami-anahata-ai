services:
  - type: web
    name: swami-anahata-backend
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    runtime: python3.10  # ✅ Force Python 3.10 to avoid building tokenizers
